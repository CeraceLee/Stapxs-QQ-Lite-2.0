user root;
worker_processes auto;
pid /run/nginx.pid;

events { }

http {
        server {
                listen 80 default_server;
                listen [::]:80 default_server;
                location / {
                        proxy_pass http://cerace;
                }
                
                location /chat/ {
                        alias /root/www/;
                        index index.html;
                }
                
                server_name _;
        }
}
